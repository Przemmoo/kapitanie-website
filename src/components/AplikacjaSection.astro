---
// Sekcja Autorskiej Aplikacji Eventowej
---

<section id="aplikacja" class="section section-secondary aplikacja-section">
  <div class="container">
    <div class="aplikacja-grid">
      <!-- Lewa strona - Mockup aplikacji -->
      <div class="app-mockup">
        <div class="phone-container">
          <div class="phone-frame">
            <div class="phone-screen">
              <div class="app-interface">
                <!-- Nagłówek aplikacji -->
                <div class="app-header">
                  <div class="app-status-bar">
                    <span class="time">9:41</span>
                    <div class="status-icons">
                      <span class="signal"></span>
                      <span class="wifi"></span>
                      <span class="battery"></span>
                    </div>
                  </div>
                  <div class="app-nav">
                    <h1 class="app-title">Tech Summit 2024</h1>
                    <div class="app-subtitle">Kraków Expo Center</div>
                  </div>
                </div>

                <!-- Główny content aplikacji -->
                <div class="app-content">
                  <!-- Menu główne -->
                  <div class="app-menu">
                    <div class="menu-item active">
                      <div class="menu-icon">🏠</div>
                      <span>Strona główna</span>
                    </div>
                    <div class="menu-item">
                      <div class="menu-icon">📅</div>
                      <span>Agenda</span>
                    </div>
                    <div class="menu-item">
                      <div class="menu-icon">👥</div>
                      <span>Networking</span>
                    </div>
                    <div class="menu-item">
                      <div class="menu-icon">📍</div>
                      <span>Mapa</span>
                    </div>
                  </div>

                  <!-- Powiadomienia -->
                  <div class="app-notifications">
                    <div class="notification">
                      <div class="notification-dot"></div>
                      <div class="notification-content">
                        <div class="notification-title">Prezentacja za 15 min</div>
                        <div class="notification-subtitle">Sala A • "Przyszłość AI"</div>
                      </div>
                    </div>
                  </div>

                  <!-- Live Schedule -->
                  <div class="app-schedule">
                    <h3>Aktualnie</h3>
                    <div class="schedule-item current">
                      <div class="schedule-time">14:00 - 14:45</div>
                      <div class="schedule-title">Keynote: Future of Technology</div>
                      <div class="schedule-speaker">Dr. Anna Kowalska</div>
                      <div class="schedule-live">
                        <span class="live-indicator"></span>
                        LIVE
                      </div>
                    </div>
                  </div>
                </div>

                <!-- Dolna nawigacja -->
                <div class="app-bottom-nav">
                  <div class="nav-item active">
                    <div class="nav-icon">🏠</div>
                  </div>
                  <div class="nav-item">
                    <div class="nav-icon">🎤</div>
                  </div>
                  <div class="nav-item">
                    <div class="nav-icon">💬</div>
                  </div>
                  <div class="nav-item">
                    <div class="nav-icon">⚙️</div>
                  </div>
                </div>
              </div>
            </div>
          </div>
          
          <!-- Floating elements around phone -->
          <div class="floating-element notification-bubble">
            <span>5 nowych wiadomości</span>
          </div>
          <div class="floating-element feature-badge">
            <span>Push notifications</span>
          </div>
          <div class="floating-element analytics-chart">
            <div class="chart-bars">
              <div class="bar"></div>
              <div class="bar"></div>
              <div class="bar"></div>
              <div class="bar"></div>
            </div>
            <span>Analityka live</span>
          </div>
        </div>
      </div>

      <!-- Prawa strona - Informacje o aplikacji -->
      <div class="app-info">
        <h2 class="section-title">
          Centrum dowodzenia w kieszeni uczestnika
        </h2>
        
        <p class="app-description">
          Wspieramy wydarzenia naszą autorską aplikacją mobilną, która zwiększa zaangażowanie 
          i ułatwia komunikację. To więcej niż agenda – to kompletne centrum dowodzenia Twojego eventu.
        </p>

        <!-- Lista funkcji -->
        <div class="features-list">
          <div class="feature">
            <div class="feature-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/>
              </svg>
            </div>
            <div class="feature-content">
              <h3>Komunikacja w czasie rzeczywistym</h3>
              <p>Push notifications, czat grupowy i prywatne wiadomości między uczestnikami.</p>
            </div>
          </div>

          <div class="feature">
            <div class="feature-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <rect x="3" y="4" width="18" height="18" rx="2" ry="2"/>
                <line x1="16" y1="2" x2="16" y2="6"/>
                <line x1="8" y1="2" x2="8" y2="6"/>
                <line x1="3" y1="10" x2="21" y2="10"/>
              </svg>
            </div>
            <div class="feature-content">
              <h3>Interaktywna agenda</h3>
              <p>Spersonalizowana agenda z możliwością zapisywania ulubionych prezentacji i przypomnieniami.</p>
            </div>
          </div>

          <div class="feature">
            <div class="feature-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/>
                <circle cx="9" cy="7" r="4"/>
                <path d="M23 21v-2a4 4 0 0 0-3-3.87"/>
                <path d="M16 3.13a4 4 0 0 1 0 7.75"/>
              </svg>
            </div>
            <div class="feature-content">
              <h3>Platform networking</h3>
              <p>Łączenie uczestników o podobnych zainteresowaniach i ułatwianie nawiązywania kontaktów biznesowych.</p>
            </div>
          </div>

          <div class="feature">
            <div class="feature-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <polyline points="22,12 18,12 15,21 9,3 6,12 2,12"/>
              </svg>
            </div>
            <div class="feature-content">
              <h3>Analityka zaangażowania</h3>
              <p>Szczegółowe raporty aktywności uczestników i popularności poszczególnych sesji.</p>
            </div>
          </div>

          <div class="feature">
            <div class="feature-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <circle cx="12" cy="12" r="10"/>
                <polyline points="12,6 12,12 16,14"/>
              </svg>
            </div>
            <div class="feature-content">
              <h3>Transmisja na żywo</h3>
              <p>Integracja z platformami streamingowymi dla uczestników zdalnych.</p>
            </div>
          </div>

          <div class="feature">
            <div class="feature-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M14.5 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7.5L14.5 2z"/>
                <polyline points="14,2 14,8 20,8"/>
                <line x1="16" y1="13" x2="8" y2="13"/>
                <line x1="16" y1="17" x2="8" y2="17"/>
                <polyline points="10,9 9,9 8,9"/>
              </svg>
            </div>
            <div class="feature-content">
              <h3>Materiały do pobrania</h3>
              <p>Centralne repozytorium prezentacji, materiałów dodatkowych i zasobów eventowych.</p>
            </div>
          </div>
        </div>

        <!-- CTA Button -->
        <div class="app-cta">
          <a href="#kontakt" class="btn btn-primary">
            Zapytaj o aplikację dla Twojego eventu
          </a>
          <div class="app-tech-stack">
            <span>Dostępna na:</span>
            <div class="tech-icons">
              <span class="tech-icon">📱 iOS</span>
              <span class="tech-icon">🤖 Android</span>
              <span class="tech-icon">💻 PWA</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .aplikacja-section {
    position: relative;
    overflow: hidden;
  }

  .aplikacja-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem;
    align-items: center;
    min-height: 500px;
  }

  /* App Mockup */
  .app-mockup {
    position: relative;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .phone-container {
    position: relative;
    filter: drop-shadow(0 20px 40px rgba(0, 0, 0, 0.3));
  }

  .phone-frame {
    width: 280px;
    height: 580px;
    background: #2C2C2E;
    border-radius: 40px;
    padding: 8px;
    position: relative;
  }

  .phone-frame::before {
    content: '';
    position: absolute;
    top: 15px;
    left: 50%;
    transform: translateX(-50%);
    width: 120px;
    height: 6px;
    background: #1C1C1E;
    border-radius: 3px;
  }

  .phone-screen {
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, #1C1C1E 0%, #2C2C2E 100%);
    border-radius: 32px;
    overflow: hidden;
    position: relative;
  }

  /* App Interface */
  .app-interface {
    height: 100%;
    display: flex;
    flex-direction: column;
    color: white;
    font-size: 12px;
  }

  .app-header {
    padding: 20px 16px 16px;
    background: linear-gradient(135deg, var(--accent-blue), #0088CC);
  }

  .app-status-bar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 16px;
    font-size: 11px;
    font-weight: 600;
  }

  .status-icons {
    display: flex;
    gap: 4px;
  }

  .signal, .wifi, .battery {
    width: 12px;
    height: 8px;
    background: white;
    border-radius: 1px;
  }

  .app-nav .app-title {
    font-size: 18px;
    font-weight: 700;
    margin: 0 0 4px 0;
  }

  .app-subtitle {
    opacity: 0.8;
    font-size: 11px;
  }

  .app-content {
    flex: 1;
    padding: 20px 16px;
    overflow: hidden;
  }

  /* App Menu */
  .app-menu {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 12px;
    margin-bottom: 20px;
  }

  .menu-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 12px 8px;
    background: rgba(255, 255, 255, 0.05);
    border-radius: 12px;
    font-size: 10px;
    transition: var(--transition-fast);
  }

  .menu-item.active {
    background: rgba(0, 169, 255, 0.2);
  }

  .menu-icon {
    font-size: 20px;
    margin-bottom: 6px;
  }

  /* Notifications */
  .app-notifications {
    margin-bottom: 20px;
  }

  .notification {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 12px;
    background: rgba(255, 193, 7, 0.1);
    border-radius: 12px;
    border-left: 3px solid #FFC107;
  }

  .notification-dot {
    width: 8px;
    height: 8px;
    background: #FFC107;
    border-radius: 50%;
    flex-shrink: 0;
  }

  .notification-title {
    font-size: 11px;
    font-weight: 600;
    margin-bottom: 2px;
  }

  .notification-subtitle {
    font-size: 9px;
    opacity: 0.7;
  }

  /* Schedule */
  .app-schedule h3 {
    font-size: 14px;
    margin-bottom: 12px;
    color: white;
  }

  .schedule-item {
    padding: 16px 12px;
    background: rgba(255, 255, 255, 0.05);
    border-radius: 12px;
    position: relative;
  }

  .schedule-item.current {
    background: rgba(0, 169, 255, 0.1);
    border: 1px solid rgba(0, 169, 255, 0.3);
  }

  .schedule-time {
    font-size: 10px;
    color: var(--accent-blue);
    font-weight: 600;
    margin-bottom: 4px;
  }

  .schedule-title {
    font-size: 12px;
    font-weight: 600;
    margin-bottom: 4px;
  }

  .schedule-speaker {
    font-size: 10px;
    opacity: 0.7;
    margin-bottom: 8px;
  }

  .schedule-live {
    display: flex;
    align-items: center;
    gap: 6px;
    font-size: 9px;
    font-weight: 600;
    color: #FF4444;
  }

  .live-indicator {
    width: 6px;
    height: 6px;
    background: #FF4444;
    border-radius: 50%;
    animation: pulse 1.5s infinite;
  }

  @keyframes pulse {
    0% { opacity: 1; }
    50% { opacity: 0.5; }
    100% { opacity: 1; }
  }

  /* Bottom Navigation */
  .app-bottom-nav {
    display: flex;
    justify-content: space-around;
    align-items: center;
    padding: 12px 16px;
    background: rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(10px);
  }

  .nav-item {
    padding: 8px;
    border-radius: 8px;
    transition: var(--transition-fast);
  }

  .nav-item.active {
    background: rgba(0, 169, 255, 0.2);
  }

  .nav-icon {
    font-size: 16px;
  }

  /* Floating Elements */
  .floating-element {
    position: absolute;
    background: var(--accent-blue);
    color: white;
    padding: 8px 12px;
    border-radius: 20px;
    font-size: 11px;
    font-weight: 600;
    white-space: nowrap;
    animation: float 3s ease-in-out infinite;
  }

  .notification-bubble {
    top: 10%;
    right: -20px;
    animation-delay: 0s;
  }

  .feature-badge {
    top: 60%;
    left: -30px;
    background: #28A745;
    animation-delay: 1s;
  }

  .analytics-chart {
    bottom: 20%;
    right: -40px;
    background: #FFC107;
    color: #333;
    display: flex;
    align-items: center;
    gap: 8px;
    animation-delay: 2s;
  }

  .chart-bars {
    display: flex;
    gap: 2px;
    align-items: end;
  }

  .bar {
    width: 3px;
    background: #333;
    border-radius: 2px;
  }

  .bar:nth-child(1) { height: 12px; }
  .bar:nth-child(2) { height: 8px; }
  .bar:nth-child(3) { height: 16px; }
  .bar:nth-child(4) { height: 10px; }

  @keyframes float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
  }

  /* App Info */
  .app-info {
    padding-left: 2rem;
  }

  .section-title {
    color: var(--text-main);
    margin-bottom: 2rem;
  }

  .app-description {
    font-size: 1.2rem;
    color: var(--text-secondary);
    line-height: 1.6;
    margin-bottom: 2rem;
  }

  /* Features List */
  .features-list {
    margin-bottom: 2rem;
  }

  .feature {
    display: flex;
    gap: 1rem;
    margin-bottom: 1rem;
    padding: 1rem;
    background: rgba(31, 31, 31, 0.3);
    border-radius: 12px;
    border: 1px solid rgba(0, 169, 255, 0.1);
    transition: var(--transition-fast);
  }

  .feature:hover {
    background: rgba(31, 31, 31, 0.6);
    border-color: rgba(0, 169, 255, 0.3);
    transform: translateX(10px);
  }

  .feature-icon {
    flex-shrink: 0;
    width: 48px;
    height: 48px;
    background: linear-gradient(135deg, var(--accent-blue), #0088CC);
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
  }

  .feature-icon svg {
    width: 24px;
    height: 24px;
  }

  .feature-content h3 {
    color: var(--text-main);
    margin: 0 0 0.5rem 0;
    font-size: 1.1rem;
  }

  .feature-content p {
    color: var(--text-secondary);
    margin: 0;
    line-height: 1.5;
  }

  /* CTA Section */
  .app-cta {
    text-align: center;
  }

  .app-cta .btn {
    margin-bottom: 1.5rem;
    padding: 15px 30px;
    font-size: 1.1rem;
  }

  .app-tech-stack {
    color: var(--text-secondary);
    font-size: 0.9rem;
  }

  .tech-icons {
    display: flex;
    justify-content: center;
    gap: 1rem;
    margin-top: 0.5rem;
  }

  .tech-icon {
    padding: 6px 12px;
    background: rgba(0, 169, 255, 0.1);
    border-radius: 20px;
    font-size: 0.8rem;
    border: 1px solid rgba(0, 169, 255, 0.2);
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .aplikacja-grid {
      gap: 3rem;
    }

    .app-info {
      padding-left: 1rem;
    }

    .phone-frame {
      width: 240px;
      height: 500px;
    }
  }

  @media (max-width: 768px) {
    .aplikacja-grid {
      grid-template-columns: 1fr;
      gap: 3rem;
    }

    .app-info {
      padding-left: 0;
    }

    .floating-element {
      display: none;
    }

    .feature {
      padding: 1rem;
    }

    .features-list .feature:hover {
      transform: none;
    }
  }

  @media (max-width: 480px) {
    .phone-frame {
      width: 200px;
      height: 420px;
    }

    .app-header {
      padding: 16px 12px 12px;
    }

    .app-content {
      padding: 16px 12px;
    }

    .feature {
      flex-direction: column;
      gap: 1rem;
      text-align: center;
    }

    .tech-icons {
      flex-direction: column;
      gap: 0.5rem;
    }
  }

  /* Performance optimizations */
  .phone-container {
    will-change: transform;
  }

  .floating-element {
    will-change: transform;
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    // Intersection Observer for animations
    const observerOptions = {
      threshold: 0.2,
      rootMargin: '0px 0px -50px 0px'
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('fade-in-up');
        }
      });
    }, observerOptions);

    // Observe elements
    document.querySelectorAll('.app-mockup, .app-info > *, .feature').forEach(el => {
      observer.observe(el);
    });

    // Phone mockup interactions
    const phoneScreen = document.querySelector('.phone-screen');
    const menuItems = document.querySelectorAll('.menu-item');
    const navItems = document.querySelectorAll('.nav-item');

    // Menu item interactions
    menuItems.forEach(item => {
      item.addEventListener('click', () => {
        menuItems.forEach(i => i.classList.remove('active'));
        item.classList.add('active');
        
        // Add subtle animation
        item.style.transform = 'scale(0.95)';
        setTimeout(() => {
          item.style.transform = '';
        }, 150);
      });
    });

    // Bottom nav interactions
    navItems.forEach(item => {
      item.addEventListener('click', () => {
        navItems.forEach(i => i.classList.remove('active'));
        item.classList.add('active');
      });
    });

    // Parallax effect for phone on desktop
    if (window.innerWidth > 768) {
      const handleScroll = () => {
        const scrolled = window.pageYOffset;
        const aplikacjaSection = document.querySelector('.aplikacja-section');
        const phoneContainer = document.querySelector('.phone-container');
        
        if (aplikacjaSection && phoneContainer) {
          const rect = aplikacjaSection.getBoundingClientRect();
          if (rect.top < window.innerHeight && rect.bottom > 0) {
            const rate = scrolled * 0.05;
            phoneContainer.style.transform = `translateY(${rate}px)`;
          }
        }
      };

      window.addEventListener('scroll', handleScroll, { passive: true });
    }

    // Auto-rotate menu items demo
    let currentMenuItem = 0;
    const autoRotateMenu = () => {
      if (window.innerWidth > 768) { // Only on desktop
        menuItems.forEach(item => item.classList.remove('active'));
        menuItems[currentMenuItem].classList.add('active');
        currentMenuItem = (currentMenuItem + 1) % menuItems.length;
      }
    };

    // Start auto-rotation when section is visible
    const startAutoRotation = () => {
      setInterval(autoRotateMenu, 3000);
    };

    const sectionObserver = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          setTimeout(startAutoRotation, 2000);
          sectionObserver.disconnect();
        }
      });
    }, { threshold: 0.5 });

    const aplikacjaSection = document.querySelector('.aplikacja-section');
    if (aplikacjaSection) {
      sectionObserver.observe(aplikacjaSection);
    }
  });
</script>
